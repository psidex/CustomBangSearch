import React from "react";

// Current structure:
// bin btn - keyword input - vert stack url input list - add url btn

// To add to the structure:
// alias input - default url input - dont encode query switch

// BangConfigurator is a component responsible for configuring a single bang in
// to go in the BangInfo array (config.bangs)
export default function BangConfigurator() {
	return <></>;
}
