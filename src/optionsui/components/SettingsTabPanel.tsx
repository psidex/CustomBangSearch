import React from 'react';

import { TabPanel } from '@chakra-ui/react';

export default function SettingsTabPanel(): React.ReactElement {
  return (
    <TabPanel>
      <p>Settings</p>
    </TabPanel>
  );
}
